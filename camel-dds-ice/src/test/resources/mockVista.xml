<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:util="http://www.springframework.org/schema/util"	
       xmlns:amq="http://activemq.apache.org/schema/core"
       xmlns:camel="http://camel.apache.org/schema/spring"
       xmlns:prop="http://camel.apache.org/schema/placeholder"
       xsi:schemaLocation="
		http://www.springframework.org/schema/beans 
			http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
			http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/util
			http://www.springframework.org/schema/util/spring-util.xsd
		http://activemq.apache.org/schema/core http://activemq.org/config/1.0/1.0.xsd
		http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd 
         ">

    <!-- 
    This module mocks Vista at the EMR API level
    It does _not_ use RPC.
    -->
    
    <camelContext id="vistaContext" xmlns="http://camel.apache.org/schema/spring">
        <!-- camel:propertyPlaceholder id="properties" location="ref:propertySources"/ -->

        <route id="mockVista">
            <from uri="direct:in" />
            <log message="##### [mockVista] request #####" />
            <to uri="log:mockVista.request?level=DEBUG&amp;showHeaders=true"/>
            <to uri="bean:patientFactory?method=createPatientResponse" />
            <log message="##### [mockVista] response #####" />
            <to uri="log:mockVista.response?level=DEBUG&amp;showHeaders=true"/>
        </route>

    </camelContext>
    
</beans>